
function setup() {
  createCanvas(400,400);
  background(0);
}

function draw() {
  fill(255);
  for(a=20; a<390; a=a+20){
      for(var b=40; b<390; b=b+20){
    ellipseMode(CENTER);
    ellipse(a,b,20,20);
      }
  }

}
