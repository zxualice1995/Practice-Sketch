function setup() {
  createCanvas(400, 400);
  background(0);

  var x = 1;
  while (x < 52) {
    stroke(180);
    line(x * 10, 10, x * 10, 390);
    line(10, x * 10, 390, x * 10);
    x = x + 1;
  }
}
