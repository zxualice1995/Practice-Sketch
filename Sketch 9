function setup() {
  createCanvas(400, 400);
  background(0);

  var x = 1;
  while (x < 40) {
    stroke(255);
    line(10, x * 10, x * 10, 10);
    line(390, 390 - x * 10, 390 - x * 10, 390);
    x = x + 1;
  }
  textSize(300);
  text("P", 110, 300);
  fill(0);
  noStroke();
}
