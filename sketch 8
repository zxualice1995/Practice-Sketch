function setup() {
  createCanvas(400, 400);
  background(0);
  stroke(255);

  for (var x = 1; x < 39; x = x + 1) {
    line(10, x * 10, x * 10, 10);
    line(390, 390 - x * 10, 390 - x * 10, 390);
  }
}
